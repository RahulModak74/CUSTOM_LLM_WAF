# Traffic-Prism WAF Dependencies

# Core WAF Framework
sanic>=23.0.0
sanic-cors>=2.0.0
aiohttp>=3.8.0
aiosqlite>=0.19.0
pydantic>=2.0.0

# Additional dependencies for enhanced features
# (automatically installed, no manual intervention needed)

# For OWASP CRS parsing enhancement
# No additional dependencies required - uses built-in Python modules

# Optional: For better performance and monitoring
# uvloop>=0.17.0  # Uncomment for better async performance on Linux
# psutil>=5.9.0   # Uncomment for system monitoring features
